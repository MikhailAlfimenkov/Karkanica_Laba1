package org.example;
import java.io.File;
import java.util.*;

    public class Twitt {
        public static void main(String[] args) {
            double totalScore;
            Twitt tweet = new Twitt(); // после того как поль1 даст данные можно убрать
            List<Twitt> tweets = new ArrayList<>(); // после того как поль1 даст данные можно убрать
            Map<String,Map<String,Double>> allAnalysis = new HashMap<>();
            String path = "C:/Users/a-servis/IdeaProjects/laba kark1/src/sentiments.csv";
            File file = new File(path);
            Map<String, Double> sumSentiments = new HashMap<>();
            List<String> wordList = new ArrayList<>();

            if (file.exists()) {
                System.out.println("файл был создан");
                try {
                    Scanner scanner = new Scanner(file);
                    while (scanner.hasNextLine()) {
                        String line = scanner.nextLine();
                        String[] words = line.split(",");

                        if (words.length >= 2) {
                            String firstElement = words[0].toLowerCase().trim();
                            Double secondElement = Double.valueOf(words[1].trim());
                            sumSentiments.put(firstElement, secondElement);
                        }
                    }
                    scanner.close();
                } catch (Exception e) {
                    System.out.println(e.getMessage());
                }
                System.out.println(sumSentiments);
                //sumSentiments.get()


            } else {
                System.out.println("файл не был создан");
            }
        }
    }




